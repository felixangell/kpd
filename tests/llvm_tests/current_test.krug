#module main

/*
	todo list:

	- next statement 
	- break statement
	- make sure next and break do not
	  cause dead code
	- make sure next/break are in 
	  looping structures?
	- make sure defer runs before
	  return statements.

*/

#{c_func, variadic}
func printf(fmt *u8) s32;

#{c_func, variadic}
func scanf(fmt *u8) s32;

#{no_mangle}
func main() s32 {
	let foo = 3;

	func() {

	}();

	let add = func() {

	};

	let result = func(a s32, b s32) {
		return a + b;
	}();

	add(5, 6);

	return 0;
}