#module main

type Node struct {
	start s32,
	end s32,
};

type PieceTable struct {
	node *Node,
};

#{no_mangle}
func main() s32 {
	let test_node Node;
	test_node.start = 5;
	test_node.end = 10;

	let table PieceTable;
	table.node = &test_node;

	let table_node_start = table.node.start;

	return 0;
}