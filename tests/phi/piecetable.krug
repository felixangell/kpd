#module piecetable

// sdl!
#{c_func}
func SDL_Init();

#{c_func}
func SDL_Quit();

#{c_func}
func SDL_Delay();

#{c_func}
func SDL_CreateWindow();

#{c_func}
func printf();

type Line s32;

type Piece_Table struct {
	a *Line, // array!
	b [Line; 32], // array!
	c (Line, Line, *Line),
	
	// TODO handle in infer!
	// d func(a Line, b Line, c Line) Line,
	// e func() Line,
	// f func(),
};

func main() {
	SDL_Init();

	let window = SDL_CreateWindow(c"Window name", 0, 0, 1280, 720, 0);
	printf(c"hello world!\n");

	SDL_Delay(2000);

	SDL_Quit();
}