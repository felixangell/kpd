#{c_func}
func printf() s32;

type Lexer struct {
	position u64,
};

func start(self *Lexer) {
	let lex Lexer = @self;
	printf(c"lexing from pos: %d\n", lex.position);
}

func main() {
	let lex Lexer;
	lex.position = 10 as u64;

	let ptr_lex *Lexer = &lex;
	start(ptr_lex);
}