#module main

#{c_func}
func printf() s32;

func main() {
	mut a f64 = 6.0;
	printf(c"a is %f\n", a);

	a = a - 3.0;
	printf(c"subsd a - 3.0 %f\n", a);

	a = a * 2.0;
	printf(c"mulsd (a * 2.0) %f\n", a);

	a = a / 2.0;
	printf(c"divsd a / 2.0 %f\n", a);
}