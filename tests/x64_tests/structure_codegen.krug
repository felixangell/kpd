#{c_func}
func printf();

type Person struct {
	age u64,
	eyebrow_count u64,
};

func main() {
	// felix is a 19 year old 
	// person with 3 eyebrows

	let felix Person;
	felix.age = 19 as u64;
	felix.eyebrow_count = 3 as u64;

	printf(c"age is %d", felix.age);
}