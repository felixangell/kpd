#load entities
#load utils::math
#load gfx::{colour, context}

type Foo int;

func main() int {
	let x = 69 - 69 / 32 << 5;
	if x == 3 && y >= x {
		for int i = 0; i < 10; i += x {
			let z = -32;
			let f = +5555;
		}
	}
}